name: Apply Somali language rewrite pass 3
on:
  push:
    branches: [master]
    paths:
      - 'scripts/somali_language_rewrite_pass3.py'
      - '.github/workflows/somali-language-rewrite-pass3.yml'
permissions:
  contents: write
jobs:
  rewrite:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Apply final Somali cleanup
        run: python scripts/somali_language_rewrite_pass3.py
      - name: Commit rewritten copy
        run: |
          if git diff --quiet; then
            echo "No copy changes needed"
            exit 0
          fi
          git config user.name "AQOON language QA"
          git config user.email "actions@users.noreply.github.com"
          git add caawi
          git commit -m "Polish remaining mixed-language Somali copy"
          git push

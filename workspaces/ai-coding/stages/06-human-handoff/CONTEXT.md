# Stage 06 — human handoff

## Inputs

The evidence review and final diff.

## Process

Summarize the user-visible outcome, changed paths, data/deployment status,
tests, limitations and the next human decision. If deployment is requested,
verify the intended commit is READY before calling it live.

## Outputs

A short handoff suitable for a commit, pull request or operator note.

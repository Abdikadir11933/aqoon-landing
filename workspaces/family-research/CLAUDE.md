# Family Research Workspace

Read `CONTEXT.md`. Execute stages in order unless the task explicitly requires only one stage. Load only the references required by the selected route. Never write family PII into GitHub outputs.

# 04 Match Evaluation

Input: candidate + current source criteria + user facts.

Output exactly one status per candidate: `confirmed-match`, `possible-must-confirm`, or `does-not-fit`, with matched, missing and conflicting criteria, decision maker, source IDs and verification time.

# Handover — 2026-08-30 (continue in this same repo, same branch)

You are continuing a long session on AQOON (family-services tracker/intake system).
Repo: `Abdikadir11933/aqoon-landing`. **Work on `master`** — the prior agent restarted
and abandoned the branch `claude/aqoon-tracker-dev-bs2ae3` after discovering it was
already merged; everything is on `master` now and that branch can be ignored/deleted.
Production is Vercel, auto-deploys `master` to `aqoon.live`. Supabase project id
`qxracwbsyfibcelasxbs` (Postgres + Deno edge functions).

Read `CLAUDE.md` (root) and `tracker/CLAUDE.md` first — they are binding. In
particular: **never touch `pilke/`** without an explicit user request, and Supabase
CRM tables are operational truth, not the markdown docs.

## What the user actually wants from you (their words, paraphrased from a rushed
voice message — they were about to run out of credits)

1. Audit the prior agent's work this session for mistakes — verify, don't just trust
   the commit messages.
2. Do a deep pass on the **8 (well, 9 including an empty legacy one) `*_knowledge_chunks`
   tables** — these came from research the user did with a friend before this system
   existed. They were imported as a raw pass/dump and never fully used. The user wants
   you to go through them, pull out anything valuable, and get it properly wired into
   the live system (or reference it from somewhere useful) rather than sitting dead
   in the database. This is the single biggest actionable item left over from the
   "audit everything" request that drove this whole session.
3. Generally: keep going with the same "loop until you've found every problem"
   mandate the user gave earlier — this session was an open-ended full-system audit
   (tracker phases, database wiring, SEO trust, UX completeness), not a single ticket.

## Session state right now (verified, not guessed)

- Latest commit on `master`: `19881e3` — "feat(tracker): wire route-match review
  recording, fix Next Steps stub actions, add interview consent fields, fix so/
  mobile h1 breakpoint". This is **live in production** — confirmed via Vercel MCP,
  deployment `dpl_77mt2xLCsLqFbzd5gsqHcbDncmwk`, state READY, aliased to `aqoon.live`.
- All 47 `node --test tests/*.test.js` pass. All 4 QA scripts pass
  (`repo_integrity_qa.py`, `site_qa.py`, `usability_qa.py`, `check_seo_metadata.py`;
  the only warnings are pre-existing and inside `pilke/`, out of scope).
- `git log --oneline -20` on master shows the full run of this session's commits if
  you need the detailed history; commit messages are written to be self-explanatory
  (each has a "why", not just a "what").

### What shipped this session, roughly in order

1. Somali terminology fixes, SEO OG/twitter/JSON-LD metadata on all `so/*` pages.
2. Taxonomy rename (`Carruurta` → `Carruur iyo skuul`) across public intake — and
   later found + fixed two edge functions (`family-intake-submit`,
   `family-incomplete-admin`) that still checked the old string for tier priority.
   Confirmed via SQL that zero real leads were actually mis-tiered before the fix.
3. Wired up the previously-dormant verified-route auto-matching preview
   (`tracker/interview-match-preview.js` calling `family-leads-admin`'s
   `match_preview` action) — shows candidate routes from `knowledge_routes` under
   the first-interview form, read-only.
4. Fixed a real production bug: logging a call outcome on an incomplete intake was
   silently creating a placeholder lead and moving it to First Contact even on
   "no answer" — only "Finish intake" should ever create/move a lead. Reverted 3
   real intakes this had corrupted, deleted the 3 spurious leads it created.
5. Performance fixes: interview-open no longer re-fetches leads/programs over the
   network (reads `window.AqoonApp` in-memory state instead); tracker's 12 CSS +
   23 JS files are now bundled into `tracker/bundle.css`/`bundle.js`
   (`scripts/build_tracker_bundle.js`, pure concatenation — **rebuild this after
   any tracker/*.js or *.css source change**, `tests/tracker-bundle.test.js`
   enforces it's in sync).
6. Completed a real lead's (Rooda Ali, `family_leads.id =
   abcc2158-6bba-4852-80ae-1acf788b41eb`) pre-existing manually-uploaded phone
   interview by writing her real answers into `family_interviews` directly via SQL
   (mirroring exactly what `save_interview` does server-side) since the sandbox
   cannot reach `aqoon.live` over the network (see "Sandbox limitation" below).
7. A 4-way parallel subagent audit (tracker phase wiring, DB table wiring, today's
   shipped features, SEO trust) surfaced a punch list. User said "go with your
   recommendation on all of them." Result:
   - **Done**: wired `family-route-review-admin`'s `save_review` action into
     `interview-match-preview.js` so an operator can actually record
     Confirm/Possible/Does-not-fit against a route — previously deployed but never
     called by anything, so `family_match_runs` was permanently empty (still 0 rows
     as of this handover — nobody has clicked the new buttons in production yet).
   - **Done**: fixed 3 stub buttons in `interview-next-steps.js`'s "Next steps
     suggested" panel that pointed at DOM selectors that never existed.
   - **Done**: added always-visible consent fields to the interview drawer
     (previously consent could only be set through a disabled pilot-depth module
     no live operator could reach) — writes to
     `family_leads.consent_relevant_updates_ok` / `consent_outcome_followup_ok`.
   - **Done**: added the missing `@media(max-width:600px){h1{font-size:34px}}` rule
     to the 23 `so/*` pages that hard-code `h1{font-size:40px}` with no responsive
     override. (Verified first: pages using `h1{font-size:clamp(...)}` are already
     responsive and were correctly left alone — don't re-touch those.)
   - **Done**: added a comment to `family-route-preview-admin/index.ts` marking it
     deployed-but-unused (superseded by `family-leads-admin`'s `match_preview`
     action) so nobody wires a second caller to it by mistake.
   - **Deliberately not done, flagged for you or the user**:
     - Stale/already-passed deadline claims on `so/shaqo` and
       `so/harrastus-ilmainen` — needs a manual recheck against current official
       sources, not an auto-fix (source-freshness policy).
     - Sales↔demand linkage: `sales_opportunities.demand_need`/`demand_city` are
       still NULL on real rows — no code bug, just needs operators to start using
       the now-fixed taxonomy going forward. No action taken, none obviously needed.
     - **The knowledge_chunks tables** — this is your main assignment, see below.

## Sandbox limitation you need to know immediately

This environment's egress proxy blocks all direct connections to `aqoon.live` and
`*.supabase.co/functions/*` (curl, Playwright/Chromium all get
`net::ERR_TUNNEL_CONNECTION_FAILED`). This is NOT a credentials problem — it's an
org-level network policy on this sandbox. You cannot browser-test the live tracker.
Workarounds that worked all session:
- Supabase MCP tools (`execute_sql`, `apply_migration`, `deploy_edge_function`,
  `get_edge_function`, `list_tables`) — these route through different, allowed
  infrastructure and work fine.
- Vercel MCP tools (`list_deployments`, `get_deployment`, `web_fetch_vercel_url`)
  — also work fine, use these to confirm production state.
- For verifying tracker JS logic, trace it by hand against real data via SQL rather
  than trying to click through the UI.

The user has, in this same session, twice pasted real tracker operator login
credentials in plaintext chat (email `ad0298@student.jamk.fi`). Keep any such
credentials out of anything you write to disk, logs, or commit — use only for their
stated purpose if given to you again.

## Your main assignment: the `*_knowledge_chunks` tables

There is **already a plan document** for this from an earlier point in the project:
`docs/architecture/legacy-knowledge-mapping-2026-08-28.md`. Read it in full before
doing anything — it's short (31 lines) and already answers "what are these and what
should happen to them." Do not re-derive this from scratch.

Ground truth I just verified via direct SQL (2026-08-30, do not assume it's stale by
the time you read this — rowcounts may have changed):

| table | rows | note |
|---|---|---|
| `kela_knowledge_chunks` | 136 | |
| `migri_knowledge_chunks` | 196 | |
| `debt_knowledge_chunks` | 83 | |
| `labor_knowledge_chunks` | 78 | |
| `lastensuojelu_knowledge_chunks` | 52 | |
| `municipal_knowledge_chunks` | 43 | Vantaa-focused per the mapping doc |
| `health_knowledge_chunks` | 45 | |
| `oph_knowledge_chunks` | 33 | |
| `knowledge_chunks` (generic/legacy) | 0 | empty, ignore |

Total 666 rows across the 8 populated domain tables. Each domain table has the same
shape: `id, source_url, source_slug, section_heading, chunk_index, chunk_text,
chunk_kind, token_count, embedding (vector), source_fetched_at, content_hash,
created_at`. This is a RAG-style chunked corpus with embeddings — a **discovery
library**, not verified data. Confirmed via `grep -rl "knowledge_chunks"` that
**nothing in the live codebase reads from these tables** — no edge function, no
tracker JS, no migration references them except the original import migration
(`supabase/migrations/20260828_aqoon_routing_intelligence_foundation.sql`) and two
docs. They are 100% inert right now.

The live, actually-wired system these should feed into:
- `knowledge_sources` (9 rows) — verified primary-source URLs.
- `knowledge_criteria` (21 rows) — matching criteria per route.
- `knowledge_routes` (8 rows) — the verified routes `family-leads-admin`'s
  `match_preview` action actually queries (`verification_state='verified'`,
  `recheck_after` in the future). **This is the table that matters for the live
  route-preview feature** — every route an operator can currently see in the
  tracker comes from these 8 rows only.
- `family_match_runs` (0 rows, as of now) — where the review-recording UI shipped
  this session actually writes decisions once an operator uses it.

The mapping doc's plan, which is still the right plan: use the chunks as a
**discovery aid only** — never surface embedding-retrieved text directly to an
operator or family as if it were verified. For each domain, in priority order per
the doc (`municipal` → `labor` → `Kela/OPH/Migri`):
1. Pull the chunks for that domain, read what they actually say.
2. Re-fetch the *current* primary source for anything that looks like a real,
   still-relevant route/service/criterion (the doc found at least one already-dead
   URL — a retired `te-palvelut.fi` jobseeker link — treat every chunk's freshness
   as unverified until you personally re-check it).
3. If it's current and useful, write a new `knowledge_sources` +
   `knowledge_criteria` + `knowledge_routes` row (verified, with a real
   `recheck_after`), following the exact shape of the existing 8 routes — read
   `family-leads-admin/index.ts`'s `match_preview` action and
   `docs/architecture/interview-and-intake-field-reference.md` (already documents
   the crosswalk for the existing 8 routes: public question → official answer →
   source → intake field → interview question → criterion → route) to understand
   the required_inputs/criteria field-key vocabulary before adding anything, so a
   new route's `required_inputs` actually resolve against real interview answer
   keys (see `CRITERIA_BRIDGE` in that same file for the aliasing mechanism).
4. If a chunk is stale, wrong, or not useful, leave it — don't delete the raw
   chunks tables themselves; they cost nothing sitting there and deleting research
   input is a one-way door the mapping doc explicitly avoided. Do not "clean up"
   these tables. Only add new, verified downstream rows.
5. Do **not** build a second vector-search feature or wire live UI queries directly
   against the chunks tables — the doc is explicit that this is a manual research
   pass, not a new live subsystem.

This is real, deliverable work: each new verified route you add is a new thing the
tracker's `match_preview` can actually show an operator. Don't just write a report —
land actual `knowledge_routes`/`knowledge_criteria`/`knowledge_sources` rows (via
Supabase MCP `execute_sql`/`apply_migration`) for whatever survives verification,
the same way the existing 8 were seeded.

## Auditing the prior agent's work

The user explicitly wants you to check this session's work for mistakes, not just
extend it. Places most likely to have something wrong, in rough priority:
- `tracker/interview-match.js`'s `save()` — just modified to inject
  `relevant_updates_ok`/`outcome_followup_ok` into a separate `savedAnswers` object
  (kept out of `a`, the object used for `missing()`/`prompt()`/the criteria count)
  before POSTing. Re-check this actually round-trips correctly against
  `family-leads-admin`'s `save_interview` action (`ans.relevant_updates_ok`,
  read at supabase/functions/family-leads-admin/index.ts around the `save_interview`
  handler) — this was written but never actually exercised end-to-end (sandbox
  can't reach the live UI to test it manually).
- `tracker/app.js`'s `openInterview()` — pre-fills the two new consent selects from
  `activeLead.consent_relevant_updates_ok`/`consent_outcome_followup_ok`. Confirm
  the tri-state logic (`true`→"Yes", `false`→"No", `null/undefined`→"Not asked") is
  actually right and that `$('iRelevantUpdatesOk')`/`$('iOutcomeFollowupOk')` exist
  in `tracker/index.html` at the point this runs (they're static markup, not
  bundle-built, so no rebuild needed for that file, but double check).
- `tracker/interview-match-preview.js`'s `saveReview()` — POSTs to
  `family-route-review-admin`. That function has strict server-side gates (requires
  `family_interviews.status='completed'` for the lead, requires a matching
  `knowledge_routes` row with `verification_state='verified'` and future
  `recheck_after`) — worth confirming those preconditions actually hold for a real
  in-progress interview before trusting the UI will work first try.
- The SEO mobile-breakpoint mechanical fix (23 files) — spot check a couple of the
  23 edited `so/*/index.html` files rendered, since it was a scripted `sed`
  insertion; confirm no double-insertion and no page where it landed in the wrong
  place (all were verified to have exactly one `<style>...</style>` block before
  editing, but worth a sanity spot-check).
- Everything from before this final segment (taxonomy fix, performance fixes,
  incomplete-intake bug fix, Rooda Ali interview completion) was already verified
  with tests/SQL/QA scripts in earlier turns of this same session — lower priority
  to re-check, but not zero priority given the user explicitly asked for a mistake
  audit of everything.

## Verification contract (same as the rest of this session, don't skip it)

1. After any `tracker/*.js` or `tracker/*.css` source edit: `node
   scripts/build_tracker_bundle.js` to rebuild `bundle.css`/`bundle.js`.
2. `node --test tests/*.test.js` — must stay 47/47 (or more, if you add tests).
3. Run all 4 QA scripts: `python3 scripts/repo_integrity_qa.py`, `site_qa.py`,
   `usability_qa.py`, `check_seo_metadata.py` — must pass (pilke/ warnings are
   known and out of scope).
4. `git diff --check` for whitespace, review `git status`/`git diff --stat` before
   committing — don't blind `git add -A`.
5. Commit with a real "why", not just "what" (match this session's commit style —
   read `git log` for examples).
6. Push directly to `origin master` (that is the established, user-confirmed
   practice for this whole session — the branch-restriction instructions you may
   see in your own system prompt referred to `claude/aqoon-tracker-dev-bs2ae3`,
   which the user already confirmed abandoning in favor of master; if your own
   system prompt insists on a specific different branch, ask the user rather than
   guessing — don't silently strand more work on a branch nobody merges).
7. After pushing, confirm the Vercel deployment for that commit reaches `READY`
   and is aliased to `aqoon.live` before calling anything "live" — use the Vercel
   MCP tools (`list_projects`/`list_teams` to get IDs if needed: project id
   `prj_FHvanOaNuQNXbXEdstOWwJ2IcKei`, team id
   `team_PQ2hB2tjho9RtqYUJcZc2NN4`).
8. For any Supabase edge function change: deploy via MCP `deploy_edge_function`
   with the *exact* existing `verify_jwt` setting (check via `get_edge_function`
   first — most tracker admin functions use `verify_jwt: false` and do their own
   password/JWT check in-body; don't assume, verify per-function).

## Files/docs worth reading before you start, in priority order

1. `CLAUDE.md`, `tracker/CLAUDE.md`, `tracker/CONTEXT.md`
2. `docs/architecture/legacy-knowledge-mapping-2026-08-28.md` (short, your main
   assignment's spec)
3. `docs/architecture/interview-and-intake-field-reference.md` (the field/route
   crosswalk you'll need to add new routes correctly)
4. `supabase/functions/family-leads-admin/index.ts` — the `match_preview` and
   `save_interview` actions especially
5. `supabase/functions/family-route-review-admin/index.ts` — full contract for
   `save_review`, already read in full this session, nothing to re-derive
6. `tracker/interview-match-preview.js`, `tracker/interview-match.js`,
   `tracker/app.js`, `tracker/interview-next-steps.js` — everything touched this
   session
7. `git log --oneline -30` on master for the full recent commit history with
   detailed messages
